function logger() {
    console.log('My name is Milana');
};

logger();

function shoppingList(item1, item2) {
    const list = (`I need to get ${item1} and ${item2}!`);
    return list
};

console.log(shoppingList('apples', 'oranges'));
